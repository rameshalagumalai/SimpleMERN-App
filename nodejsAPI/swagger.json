{
    "swagger": "2.0",
    "info": {
      "version": "1.0.0",
      "title": "My User Project CRUD",
      "description": "My User Project Application API",
      "license": {
        "name": "MIT",
        "url": "https://opensource.org/licenses/MIT"
      }
    },
    "host": "localhost:28908",
    "basePath": "/",
    "tags": [
      {
        "name": "Task Manager",
        "description": "API for the task manager application"
      }
    ],
    "schemes": ["http"],
    "consumes": ["application/json"],
    "produces": ["application/json"],
    "paths": {
        "/api/Task/tasks": {
          "get": {
            "tags": ["Tasks"],
            "summary": "Get all tasks to be done",
            "responses": {
              "200": {
                "description": "OK",
                "schema": {
                  "$ref": "#/definitions/Tasks"
                }
              }
            }
          }
        },
        "/api/task/add": {
            "post": {
              "tags": ["Tasks"],
              "description": "Add a new task",
              "parameters": [
                {
                  "name": "task",
                  "in": "body",
                  "description": "User that we want to create",
                  "schema": {
                    "$ref": "#/definitions/Task"
                  }
                }
              ],
              "produces": ["application/json"],
              "responses": {
                "200": {
                  "description": "New task is added",
                  "schema": {
                    "$ref": "#/definitions/Task"
                  }
                }
              }
            }
          }
      },
     "definitions": {
        "Task": {
          "required": ["taskName"],
          "properties": {
            "taskName": {
              "type": "string"
            }
          }
        },
        "Tasks": {
          "type": "array",
          "$ref": "#/definitions/Task"
        }
      }
  }